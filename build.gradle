// Top-level build file
plugins {
    id 'com.android.application' version '8.2.0' apply false
}

// Clean task using the new Gradle APIs
tasks.register('clean', Delete) {
    delete rootProject.buildDir
}